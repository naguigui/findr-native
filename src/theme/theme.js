import * as Colors from './colors'

const theme = {
	colors: {
		main: Colors.MAIN_BLUE,
		white: Colors.WHITE,
		cloud_burst: Colors.CLOUD_BURST,
		grey: Colors.GREY,
		red: Colors.RED,
	},
}

export default theme
