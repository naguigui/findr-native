import styled from 'styled-components/native'

export const RestaurantSelectionWrapper = styled.View`
	display: flex;
	flex-direction: column;
	flex: 1;
`

export const Content = styled.View`
	display: flex;
	flex-direction: column;
	max-height: 300;
`
